const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>n==null?'—':n.toLocaleString('en-US'),signed=n=>n==null?'—':(n>0?'+':'')+fmt(n);let DATA,selected='coding-agents',sort='count',asc=false,showAll=false;
const release=t=>t.windows[$('release-range').value],attention=t=>t.windows[$('star-range').value];
function spark(a,stars=false){const max=Math.max(1,...a.filter(n=>n!=null));return `<div class="spark" aria-label="${stars?"Weekly new stars":"Two-week release counts"}: ${a.map(n=>n==null?'not covered':n).join(', ')}">${a.map(n=>`<i class="${n==null?'missing':n===0?'zero':''}" style="height:${n==null?0:n/max*100}%" title="${n==null?'History unavailable':n+(stars?' new stars':' releases')}"></i>`).join('')}</div>`}
function rate(a){return a.growthRate==null?(a.stars>0?'New baseline':'—'):'≈ '+a.growthRate.toFixed(1)+'%'+(a.baseline<20?' · small baseline':'')}
function cumulativeChart(a){if(!a.repos.length)return '';const counts=Array.from({length:13},(_,i)=>a.repos.reduce((s,r)=>s+r.weekly[i],0));let total=0;const cumulative=counts.map(n=>total+=n),max=Math.max(1,...cumulative),path=cumulative.map((n,i)=>`${i?'L':'M'}${25+i*28},${135-n/max*110}`).join(' ');return `<div class="detail-chart"><h3>Attention over time</h3><p class="small">Cumulative stars added over the last 13 weeks</p><svg viewBox="0 0 390 166" role="img" aria-label="Cumulative new stars: ${cumulative.join(', ')}"><line x1="25" x2="365" y1="135" y2="135" stroke="#ddd"/><path d="${path}" fill="none" stroke="#2861ed" stroke-width="2.5"/><text x="365" y="19" text-anchor="end">+${fmt(total)}</text><text x="25" y="156">13 weeks ago</text><text x="365" y="156" text-anchor="end">${DATA.asOf}</text></svg></div>`}
function driverDetail(){const t=DATA.topics.find(x=>x.id===selected)||DATA.topics[0],r=release(t),a=attention(t),top=a.repos[0];$('detail').innerHTML=`<span class="eyebrow">Topic focus</span><h2>${esc(t.name)}</h2><p class="intro">${esc(t.description)}</p><a class="all" href="../#library?direction=${encodeURIComponent(t.id)}">View ${t.library} entries in Library ↗</a><div class="detailsection"><h3>What drives attention?</h3><div class="bigvalue">${a.stars==null?'—':'+'+fmt(a.stars)} <small>recorded stars · ${$('star-range').selectedOptions[0].text}</small></div><div class="notice">${a.stars?`<strong>${esc(top.name)}</strong> contributes <strong>${Math.round(a.share*100)}%</strong>. Other repositories contribute <strong>+${fmt(a.otherStars)}</strong>.${a.share>=.8?' Attention is concentrated in one repository.':''}`:'No comparable growth has been recorded for this sample.'}<br>${a.repos.length?`${a.active} of ${a.repos.length} covered repositories gained stars.`:'Repository history is missing, not zero.'}${a.stars?` At least ${Math.floor(a.newRepoStars/a.stars*100)}% comes from new repositories.`:''}</div><p class="small">Growth: ${rate(a)} · ${fmt(a.baseline)} recorded stars before the period. This is event growth, not net change in current stargazers.</p>${cumulativeChart(a)}${a.repos.slice(0,showAll?a.repos.length:4).map(x=>`<div class="repo"><div class="repotop"><a href="${esc(x.url)}" target="_blank" rel="noopener">${esc(x.name)}</a><strong>+${fmt(x.stars)}</strong></div><div class="repobar"><i style="width:${a.stars?x.stars/a.stars*100:0}%"></i></div><div class="repometa"><span>${esc(x.names.slice(0,2).join(' · '))}</span><span>${a.stars?Math.round(x.stars/a.stars*100):0}%</span></div></div>`).join('')}${a.repos.length>4?`<button class="all" id="showRepos">${showAll?'Show fewer':'Show all '+a.repos.length+' repositories'}</button>`:''}</div><div class="detailsection"><h3>What was released</h3><p class="small">${r.count==null?r.observedCount+' observed · incomplete coverage':r.count+' new families'} · ${r.start} – ${DATA.asOf}</p>${r.releases.slice(0,6).map(x=>`<article class="release"><div class="releasehead"><a href="${esc(x.url||'../#library?direction='+t.id)}" target="_blank" rel="noopener">${esc(x.name)}</a><time>${x.date}</time></div><p>${esc(x.description.slice(0,170))}${x.description.length>170?'…':''}</p></article>`).join('')||'<p class="small">No dated releases recorded in this period.</p>'}</div><div class="detailsection"><h3>Use in model reports</h3><p class="small">Current tracked evidence · not adoption growth</p>${t.trackedUse.length?t.trackedUse.slice(0,6).map(x=>`<div class="release"><strong>${esc(x.name)}</strong><p class="small">${x.reports.map(y=>`<a href="${esc(y.url||y.sourceUrl)}" target="_blank" rel="noopener">${esc(y.provider)}</a>`).join(' · ')}</p></div>`).join(''):'<p class="small">No source-linked model-report use is tracked for this topic yet.</p>'}</div>`;if($('showRepos'))$('showRepos').onclick=()=>{showAll=!showAll;detail()}}
document.addEventListener('click',e=>{const b=e.target.closest('[data-select]');if(!b)return;selected=b.dataset.select;showAll=false;render();if(innerWidth<781)$('detail').scrollIntoView({behavior:'smooth',block:'start'})});document.querySelectorAll('[data-sort]').forEach(b=>b.onclick=()=>{if(sort===b.dataset.sort)asc=!asc;else{sort=b.dataset.sort;asc=false}render()});$('search').oninput=render;['release-range','star-range'].forEach(id=>$(id).onchange=render);
fetch('../data/trends_topics.json').then(r=>{if(!r.ok)throw Error('Data unavailable');return r.json()}).then(data=>{DATA=data;window.trendsData=data;$('date').innerHTML=`<b>Through ${data.asOf}</b>Last complete source week`;$('summary').innerHTML=[['Research topics',data.topics.length,'Same classification across the site'],['Covered repositories',data.coverage.completeHistories,'Complete histories in these topics'],['Tracked report use',new Set(data.topics.flatMap(t=>t.trackedUse.map(r=>r.id))).size,'Benchmarks with source-linked use']].map(([label,value,note])=>`<div class="summarycard"><h3>${label}</h3><div class="headline">${value}</div><span class="small">${note}</span></div>`).join('');$('method').innerHTML='<p>Release coverage begins '+data.releaseCoverageStart+'. Counts describe the indexed collection, not every benchmark published worldwide.</p>'+Object.entries(data.methodology).map(([k,v])=>`<p><strong>${esc(k)}:</strong> ${esc(v)}</p>`).join('');render()}).catch(error=>{console.error('Trends initialization failed:',error);$('rows').innerHTML='<div class="empty">Trend data could not be loaded. Please refresh.</div>'});
let mode='release';
function values(t){const r=release(t),a=attention(t);return mode==='release'?{count:r.count,delta:r.delta,growthRate:r.previous>0&&r.delta!=null?r.delta/r.previous*100:null,bars:r.bars}: {count:a.stars,delta:a.stars==null?null:a.stars-a.repos.reduce((s,x)=>s+x.previous,0),growthRate:a.growthRate,bars:Array.from({length:13},(_,i)=>a.repos.length?a.repos.reduce((s,x)=>s+x.weekly[i],0):null)}}
function render(){if(!DATA)return;const items=DATA.topics.filter(t=>t.name.toLowerCase().includes($('search').value.toLowerCase().trim())).sort((a,b)=>{const x=values(a)[sort],y=values(b)[sort];return x==null?(y==null?a.name.localeCompare(b.name):1):y==null?-1:(asc?1:-1)*(x-y)||a.name.localeCompare(b.name)});const current=mode==='release'?release(DATA.topics[0]):attention(DATA.topics[0]);$('ranking-title').textContent='Research directions · '+(mode==='release'?'New releases':'Attention growth');$('ranking-note').textContent=current.start+' – '+DATA.asOf+' · compared with the preceding period';$('metric-title').textContent=mode==='release'?'New':'New stars';$('spark-title').textContent=mode==='release'?'16-week trend':'13-week trend';document.querySelectorAll('[data-sort]').forEach(b=>{const active=b.dataset.sort===sort;b.querySelector('i').textContent=active?(asc?'↑':'↓'):'↕';b.style.color=active?'#171717':'';b.setAttribute('aria-label',b.textContent.replace(/[↑↓↕]/g,'').trim()+(active?(asc?', ascending':', descending'):', sort'))});$('rows').innerHTML=items.map(t=>{const v=values(t),a=attention(t);return `<button class="row ${selected===t.id?'selected':''}" data-select="${t.id}" aria-pressed="${selected===t.id}"><span class="topic">${esc(t.name)}<span class="sub">${mode==='release'?t.library+' Library entries':a.repos.length+' covered repositories'}</span></span><span class="number">${fmt(v.count)}</span><span class="delta ${v.delta>0?'up':''}">${signed(v.delta)}</span><span class="rate-value" title="${mode==='release'?'Change in release count / preceding period releases':'New recorded stars / recorded stars before the period'}">${v.growthRate==null?'—':(mode==='star'?'≈ ':'')+(v.growthRate>0?'+':'')+v.growthRate.toFixed(1)+'%'}</span>${spark(v.bars,mode==='star')}</button>`}).join('')||'<div class="empty">No matching research directions.</div>';const ranked=[...DATA.topics].sort((a,b)=>(values(b).count??-1)-(values(a).count??-1)),lead=ranked[0];const changed=DATA.topics.some(t=>values(t).delta!=null)?DATA.topics.filter(t=>(values(t).delta??0)>0).length:'—';const cards=mode==='release'?[['Research directions',DATA.topics.length,'Explore the indexed research landscape'],['Directions increasing',changed,'More releases than the previous period'],['Most new releases',fmt(values(lead).count),lead.name]]:[['Covered repositories',DATA.coverage.completeHistories,'Complete histories across research topics'],['Directions accelerating',changed,'More new stars than the previous period'],['Most new stars','+'+fmt(values(lead).count),lead.name]];$('summary').innerHTML=cards.map(([label,n,note])=>`<div class="summarycard"><h3>${label}</h3><div class="headline">${n}</div><span class="small">${esc(note)}</span></div>`).join('');detail()}
function detail(){if(mode==='star'){driverDetail();return}const t=DATA.topics.find(x=>x.id===selected)||DATA.topics[0],r=release(t),bars=spark(r.bars).replace(/title="(\d+) releases"><\/i>/g,'title="$1 releases"><b>$1</b></i>');$('detail').innerHTML=`<span class="eyebrow">Direction focus</span><h2>${esc(t.name)}</h2><p class="intro">${esc(t.description)}</p><div class="topic-chart"><h3>New benchmark families</h3><p class="small">Eight two-week periods · ${r.count==null?'Selected period has incomplete coverage':r.count+' releases in the selected period'}</p>${bars}<div class="chart-label"><span>16 weeks ago</span><span>${DATA.asOf}</span></div></div><div class="detailsection"><h3>Recent releases</h3><p class="small">${r.start} – ${DATA.asOf}</p>${r.releases.slice(0,8).map(x=>`<article class="release"><div class="releasehead"><a href="${esc(x.url||'../#library?direction='+t.id)}" target="_blank" rel="noopener">${esc(x.name)}</a><time>${x.date}</time></div><p>${esc(x.description.slice(0,190))}${x.description.length>190?'…':''}</p></article>`).join('')||'<p class="small">No dated releases recorded for this period.</p>'}</div><a class="all" href="../#library?direction=${encodeURIComponent(t.id)}">View ${t.library} entries in Library ↗</a>`}
function chooseMetric(next){mode=next;sort='count';asc=false;showAll=false;$('releaseTab').classList.toggle('active',mode==='release');$('starTab').classList.toggle('active',mode==='star');$('releaseTab').setAttribute('aria-pressed',String(mode==='release'));$('starTab').setAttribute('aria-pressed',String(mode==='star'));$('period').value=$(mode==='release'?'release-range':'star-range').value;render()}
$('releaseTab').onclick=()=>chooseMetric('release');$('starTab').onclick=()=>chooseMetric('star');$('period').onchange=()=>{$(mode==='release'?'release-range':'star-range').value=$('period').value;render()};try{$('saved-count').textContent=JSON.parse(localStorage.getItem('benchmark-radar:watchlist:v1')||'[]').length}catch{};
