const state={window:"30d",sort:"attention",search:"",domain:"",visible:60,benchmarks:[],manifest:null,library:[],libraryManifest:null,librarySearch:"",libraryDomain:"",libraryVisible:60,trends:null,savedOnly:false,saved:new Set(JSON.parse(localStorage.getItem("benchmark-radar:watchlist:v1")||"[]"))};
window.benchmarkRadarState=state;
const $=id=>document.getElementById(id);const escapeHtml=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmt=n=>n==null?"—":Number(n).toLocaleString();
const readinessLabel=value=>value==="Inspectable"?"Protocol available":value;
function route(){const name=location.hash==="#trends"?"trends":location.hash==="#library"?"library":location.hash==="#saved"?"saved":"radar";state.savedOnly=name==="saved";if(state.savedOnly){state.window="90d";document.querySelectorAll("#window-controls button").forEach(b=>b.classList.toggle("active",b.dataset.window==="90d"));}$("radar-view").hidden=name==="trends"||name==="library";$("library-view").hidden=name!=="library";$("trends-view").hidden=name!=="trends";document.querySelectorAll("[data-route]").forEach(a=>a.classList.toggle("active",a.dataset.route===name));if(name==="trends")renderTrend();else if(name==="library")renderLibrary();else renderRadar();}
function eligible(r){const latest=state.manifest.latestSourceDate||state.manifest.dataAsOf;if(state.window==="today")return r.releasedAt===latest||r.attention?.hfDailySubmittedAt?.slice(0,10)===state.manifest.dataAsOf;const age=(new Date(state.manifest.dataAsOf)-new Date(r.releasedAt))/86400000;return age>=0&&age<=Number(state.window.slice(0,-1));}
function compactSignal(r){if(r.source?.type==="library"&&r.modelReportReferences?.length)return `Used in ${r.modelReportReferences.length} tracked model report${r.modelReportReferences.length===1?"":"s"}`;if(r.attention?.hfPaperUpvotes!=null)return `${fmt(r.attention.hfPaperUpvotes)} HF votes`;if(r.attention?.githubStars!=null)return `${fmt(r.attention.githubStars)} GitHub stars`;if(r.attention?.hfDatasetDownloads!=null)return `${fmt(r.attention.hfDatasetDownloads)} dataset downloads`;return r.source?.type==="library"?"Reviewed Library record":"Attention not measured yet";}
function publicationLabel(r){const p=r.publication;if(!p||p.status==="unverified")return "";const label=p.status==="published"?"Published":p.status==="accepted"?"Accepted":p.status==="acceptance_claimed"?"Acceptance claimed":"Publication reported";return `${label}${p.venue?` · ${p.venue}`:""}`;}
function watchLabel(r){return r.watch?.status==="watch"?`Watch${r.watch.horizon?` · ${r.watch.horizon}`:""}`:"";}
function resource(label,url){return url?`<a href="${escapeHtml(url)}" target="_blank" rel="noreferrer">${label} ↗</a>`:"";}
function displayEligible(r){return r.evaluationMode!=="viewpoint_probe";}
function librarySignal(r){const adoption=r.detail?.adoption?.independentOrganizations?.length||0;if(adoption)return `${adoption} independently tracked organization${adoption===1?"":"s"}`;if(r.modelReportReferences?.length)return `Referenced in ${r.modelReportReferences.length} tracked model report${r.modelReportReferences.length===1?"":"s"}`;return readinessLabel(r.readiness)||"Public benchmark";}
function card(r,surface){const rank=r.ranking?.[state.window];const saved=state.saved.has(r.id);const library=surface!=="radar";const date=new Date(`${r.releasedAt}T00:00:00Z`).toLocaleDateString("en-GB",{day:"2-digit",month:"short",timeZone:"UTC"});const publication=publicationLabel(r),watch=watchLabel(r);const leading=library?`<div class="card-context"><small>AREA</small><span>${escapeHtml(r.area||"Benchmark")}</span></div>`:`<div class="card-date"><small>${r.source?.type==="arxiv"?"Paper v1":"Released"}</small>${date}</div>`;const signal=library?librarySignal(r):(rank?.rank?`Attention #${rank.rank} · ${state.window}`:compactSignal(r));const extra=library&&r.detail?.leaderboard?.bestScore!=null?`<span>Best comparable ${escapeHtml(r.detail.leaderboard.bestScore)}</span>`:rank?.rank?`<span>${compactSignal(r)}</span>`:"";return `<article class="benchmark-card ${library?"library-card":"radar-card"}" data-id="${r.id}" data-surface="${surface}">${leading}<div><div class="tags"><span>${escapeHtml(r.primaryDomain)}</span>${watch&&!library?`<span class="watch-tag" title="Experimental forecast of independent adoption, not a quality endorsement.">${escapeHtml(watch)}</span>`:""}${publication?`<span class="publication-tag">${escapeHtml(publication)}</span>`:""}${(r.topics||[]).slice(0,1).map(t=>`<span>${escapeHtml(t)}</span>`).join("")}</div><h2>${escapeHtml(r.name)}</h2><p class="one-line">${escapeHtml(r.oneLine)}</p><div class="meta"><span class="readiness">${escapeHtml(readinessLabel(r.readiness))}</span><span>${escapeHtml(signal)}</span>${extra}</div><div class="resources">${resource("Paper",r.links?.report)}${resource("Project",r.links?.project)}${resource("Code",r.links?.code)}${resource("Data",r.links?.data)}${library?resource("Leaderboard",r.detail?.leaderboardUrl):resource("HF",r.links?.hfPaper)}</div></div><div class="card-actions"><button class="save ${saved?"saved":""}" aria-pressed="${saved}">${saved?"Saved":"Save"}</button><button class="details" aria-expanded="false">View details ↓</button></div><div class="details-panel" hidden></div></article>`;}
function renderRadar(){if(!state.manifest)return;const source=state.savedOnly?state.library:state.benchmarks;let rows=source.filter(displayEligible).filter(r=>state.savedOnly||eligible(r)).filter(r=>!state.savedOnly||state.saved.has(r.id)).filter(r=>!state.domain||r.primaryDomain===state.domain).filter(r=>!state.search||[r.name,r.oneLine,r.primaryDomain,...(r.topics||[])].join(" ").toLowerCase().includes(state.search));const priority=r=>r.ranking?.[state.window]?.score??-1;rows.sort((a,b)=>state.sort==="newest"?b.releasedAt.localeCompare(a.releasedAt):priority(b)-priority(a)||b.releasedAt.localeCompare(a.releasedAt));$("benchmark-list").innerHTML=rows.length?rows.slice(0,state.visible).map(r=>card(r,state.savedOnly?"library":"radar")).join(""):`<div class="empty">${state.savedOnly?"No saved benchmarks. Save one from Radar or Library to keep it here.":"No benchmarks in this view."}</div>`;$("result-count").textContent=`${rows.length} results`;$("feed-title").textContent=state.savedOnly?"Saved benchmarks":state.sort==="newest"?"New benchmark releases":"Rising benchmarks";$("load-more").hidden=state.visible>=rows.length;$("updated").textContent=`Updated ${state.manifest.dataAsOf}`;$("summary").innerHTML=state.savedOnly?`<strong>${state.saved.size}</strong><span>saved in<br>this browser</span>`:`<strong>${state.manifest.recordCount}</strong><span>releases indexed<br>past 90 days</span>`;$("saved-count").textContent=state.saved.size;bindCards();}
function renderLibrary(){if(!state.libraryManifest)return;let rows=state.library.filter(displayEligible).filter(r=>!state.libraryDomain||r.primaryDomain===state.libraryDomain).filter(r=>!state.librarySearch||[r.name,r.oneLine,r.primaryDomain,...(r.topics||[])].join(" ").toLowerCase().includes(state.librarySearch));const recognition=r=>(r.detail?.adoption?.independentOrganizations?.length||0)*100+(r.modelReportReferences?.length||0);rows.sort((a,b)=>recognition(b)-recognition(a)||a.name.localeCompare(b.name));$("library-list").innerHTML=rows.length?rows.slice(0,state.libraryVisible).map(r=>card(r,"library")).join(""):'<div class="empty">No benchmarks match this search.</div>';$("library-count").textContent=`${rows.length} results`;$("library-more").hidden=state.libraryVisible>=rows.length;$("library-summary").innerHTML=`<strong>${rows.length}</strong><span>public benchmarks<br>in the Library</span>`;bindCards();}
function bindCards(){document.querySelectorAll(".benchmark-card").forEach(el=>{const id=el.dataset.id;el.querySelector(".save").onclick=e=>{if(state.saved.has(id))state.saved.delete(id);else state.saved.add(id);localStorage.setItem("benchmark-radar:watchlist:v1",JSON.stringify([...state.saved]));e.currentTarget.classList.toggle("saved",state.saved.has(id));e.currentTarget.textContent=state.saved.has(id)?"Saved":"Save";e.currentTarget.setAttribute("aria-pressed",String(state.saved.has(id)));$("saved-count").textContent=state.saved.size;if(state.savedOnly)renderRadar();};el.querySelector(".details").onclick=e=>{const p=el.querySelector(".details-panel"),open=p.hidden;if(open&&!p.dataset.rendered){const records=el.dataset.surface==="radar"?state.benchmarks:state.library;const record=records.find(item=>item.id===id);window.renderBenchmarkDetails?.(record,p,el.dataset.surface);p.dataset.rendered="true";}p.hidden=!open;e.currentTarget.textContent=open?"Close details ↑":"View details ↓";e.currentTarget.setAttribute("aria-expanded",String(open));};});}
function lineSvg(points){const w=900,h=270,p={l:38,r:18,t:16,b:32},max=Math.max(1,...points.map(x=>x.count)),x=i=>p.l+(points.length===1?0:i*(w-p.l-p.r)/(points.length-1)),y=v=>p.t+(max-v)*(h-p.t-p.b)/max;const coords=points.map((d,i)=>`${x(i)},${y(d.count)}`).join(" ");const area=`${p.l},${h-p.b} ${coords} ${x(points.length-1)},${h-p.b}`;const grid=[0,.5,1].map(q=>`<line class="grid-line" x1="${p.l}" y1="${y(max*q)}" x2="${w-p.r}" y2="${y(max*q)}"/><text class="axis-label" x="0" y="${y(max*q)+4}">${Math.round(max*q)}</text>`).join("");const labels=points.map((d,i)=>i===0||i===points.length-1||i===Math.floor(points.length/2)?`<text class="axis-label" text-anchor="middle" x="${x(i)}" y="${h-8}">${d.week.slice(5)}</text>`:"").join("");const dots=points.map((d,i)=>`<circle class="chart-dot" cx="${x(i)}" cy="${y(d.count)}" r="3"><title>${d.week}: ${d.count}</title></circle>`).join("");return `<svg viewBox="0 0 ${w} ${h}" aria-hidden="true">${grid}<polygon class="chart-area" points="${area}"/><polyline class="chart-line" points="${coords}"/>${dots}${labels}</svg>`;}
function renderTrend(){if(!state.trends?.domains?.length)return;const domain=$("trend-domain").value||state.trends.domains[0]?.domain;const item=state.trends.domains.find(d=>d.domain===domain)||state.trends.domains[0];const days=Number(document.querySelector("#trend-window .active")?.dataset.days||90);const usage=document.querySelector("#trend-signal .active")?.dataset.signal==="usage";const points=(usage?item.weeklyObservedUses:item.weeklyNewFamilies).slice(days===30?-5:0);const recent=usage?item.recentUsage30:item.recent30,previous=usage?item.previousUsage30:item.previous30;$("chart-title").textContent=item.domain;$("chart-signal-label").textContent=usage?"SOURCE-LINKED BENCHMARK USES":"NEW BENCHMARK FAMILIES";$("chart-source-note").textContent=usage?"Source-linked evidence · weekly deduplication":"First public release date · weekly counts";$("recent-count").textContent=fmt(recent);const pct=previous?Math.round((recent-previous)*100/previous):null;$("growth-value").textContent=pct==null?(recent?"New":"—"):`${pct>=0?"+":""}${pct}%`;$("trend-confidence").textContent=usage?`${item.independentOrganizations60d} organizations · 60d`:`${item.confidence} confidence · ${item.evidenceCount60d} releases / 60d`;$("line-chart").innerHTML=lineSvg(points);$("domain-table").innerHTML=state.trends.domains.slice(0,10).map(d=>{const current=usage?d.recentUsage30:d.recent30,prior=usage?d.previousUsage30:d.previous30;const change=prior?Math.round((current-prior)*100/prior):null;return `<div class="domain-row"><strong>${escapeHtml(d.domain)}</strong><span>${current} ${usage?"observed uses":"releases"} · 30d</span><span class="growth">${change==null?(current?"New":"—"):`${change>=0?"+":""}${change}%`}</span></div>`}).join("");}
function setup(){const domains=[...new Set(state.benchmarks.map(r=>r.primaryDomain))].sort(),libraryDomains=[...new Set(state.library.map(r=>r.primaryDomain))].sort();$("domain").innerHTML+=domains.map(d=>`<option>${escapeHtml(d)}</option>`).join("");$("library-domain").innerHTML+=libraryDomains.map(d=>`<option>${escapeHtml(d)}</option>`).join("");$("trend-domain").innerHTML=state.trends.domains.map(d=>`<option>${escapeHtml(d.domain)}</option>`).join("");document.querySelectorAll("#window-controls button").forEach(b=>b.onclick=()=>{state.window=b.dataset.window;state.visible=60;document.querySelectorAll("#window-controls button").forEach(x=>x.classList.toggle("active",x===b));renderRadar();});document.querySelectorAll("#sort-controls button").forEach(b=>b.onclick=()=>{state.sort=b.dataset.sort;document.querySelectorAll("#sort-controls button").forEach(x=>x.classList.toggle("active",x===b));renderRadar();});$("filter-toggle").onclick=()=>{$("filter-panel").hidden=!$("filter-panel").hidden;};$("search").oninput=e=>{state.search=e.target.value.toLowerCase();renderRadar();};$("domain").onchange=e=>{state.domain=e.target.value;renderRadar();};$("clear-filters").onclick=()=>{state.search="";state.domain="";$("search").value="";$("domain").value="";renderRadar();};$("load-more").onclick=()=>{state.visible+=60;renderRadar();};$("library-search").oninput=e=>{state.librarySearch=e.target.value.toLowerCase();state.libraryVisible=60;renderLibrary();};$("library-domain").onchange=e=>{state.libraryDomain=e.target.value;state.libraryVisible=60;renderLibrary();};$("library-more").onclick=()=>{state.libraryVisible+=60;renderLibrary();};$("trend-domain").onchange=renderTrend;document.querySelectorAll("#trend-window button,#trend-signal button").forEach(b=>b.onclick=()=>{b.parentElement.querySelectorAll("button").forEach(x=>x.classList.toggle("active",x===b));renderTrend();});window.addEventListener("hashchange",route);renderRadar();renderLibrary();route();}
Promise.all([fetch("./data/benchmarks_index.json").then(r=>r.json()),fetch("./data/library_index.json").then(r=>r.json()),fetch("./data/domain_trends.json").then(r=>r.json())]).then(([b,l,t])=>{state.benchmarks=b.records;state.manifest=b.manifest;state.library=l.records;state.libraryManifest=l.manifest;state.trends=t;setup();}).catch(()=>{$("benchmark-list").innerHTML='<div class="empty">Data could not be loaded. Please refresh later.</div>';});
