const state={window:"30d",sort:"attention",search:"",domain:"",visible:60,benchmarks:[],manifest:null,trends:null,savedOnly:false,saved:new Set(JSON.parse(localStorage.getItem("benchmark-radar:watchlist:v1")||"[]"))};
window.benchmarkRadarState=state;
const $=id=>document.getElementById(id);const escapeHtml=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmt=n=>n==null?"—":Number(n).toLocaleString();
function route(){const name=location.hash==="#trends"?"trends":location.hash==="#saved"?"saved":"radar";state.savedOnly=name==="saved";if(state.savedOnly){state.window="90d";document.querySelectorAll("#window-controls button").forEach(b=>b.classList.toggle("active",b.dataset.window==="90d"));}$("radar-view").hidden=name==="trends";$("trends-view").hidden=name!=="trends";document.querySelectorAll("[data-route]").forEach(a=>a.classList.toggle("active",a.dataset.route===name));if(name==="trends")renderTrend();else renderRadar();}
function eligible(r){const latest=state.manifest.latestSourceDate||state.manifest.dataAsOf;if(state.window==="today")return r.releasedAt===latest||r.attention?.hfDailySubmittedAt?.slice(0,10)===state.manifest.dataAsOf;const age=(new Date(state.manifest.dataAsOf)-new Date(r.releasedAt))/86400000;return age>=0&&age<=Number(state.window.slice(0,-1));}
function compactSignal(r){if(r.attention?.hfPaperUpvotes!=null)return `${fmt(r.attention.hfPaperUpvotes)} HF votes`;if(r.attention?.githubStars!=null)return `${fmt(r.attention.githubStars)} GitHub stars`;if(r.attention?.hfDatasetDownloads!=null)return `${fmt(r.attention.hfDatasetDownloads)} dataset downloads`;return "No public signal yet";}
function publicationLabel(r){const p=r.publication;if(!p||p.status==="unverified")return "";const label=p.status==="published"?"Published":p.status==="accepted"?"Accepted":p.status==="acceptance_claimed"?"Acceptance claimed":"Publication reported";return `${label}${p.venue?` · ${p.venue}`:""}`;}
function watchLabel(r){return r.watch?.status==="watch"?`Watch${r.watch.horizon?` · ${r.watch.horizon}`:""}`:"";}
function resource(label,url){return url?`<a href="${escapeHtml(url)}" target="_blank" rel="noreferrer">${label} ↗</a>`:"";}
function card(r){const rank=r.ranking?.[state.window];const saved=state.saved.has(r.id);const date=new Date(`${r.releasedAt}T00:00:00Z`).toLocaleDateString("en-GB",{day:"2-digit",month:"short",timeZone:"UTC"});const dateLabel=r.source?.type==="arxiv"?"Paper v1":"Released";const dsRank=rank?.datasetDownloadRank?`#${rank.datasetDownloadRank} of ${rank.datasetRankPopulation}`:"Not ranked";const publication=publicationLabel(r),watch=watchLabel(r);return `<article class="benchmark-card" data-id="${r.id}"><div class="card-date"><small>${dateLabel}</small>${date}</div><div><div class="tags"><span>${escapeHtml(r.primaryDomain)}</span>${watch?`<span class="watch-tag" title="Experimental forecast of independent adoption, not a quality endorsement.">${escapeHtml(watch)}</span>`:""}${publication?`<span class="publication-tag">${escapeHtml(publication)}</span>`:""}${(r.topics||[]).slice(0,1).map(t=>`<span>${escapeHtml(t)}</span>`).join("")}</div><h2>${escapeHtml(r.name)}</h2><p class="one-line">${escapeHtml(r.oneLine)}</p><div class="meta"><span class="readiness">${escapeHtml(r.readiness)}</span><span>${rank?.rank?`Attention #${rank.rank} · ${state.window}`:compactSignal(r)}</span>${rank?.rank?`<span>${compactSignal(r)}</span>`:""}</div><div class="resources">${resource("Source",r.links?.report)}${resource("PDF",r.links?.pdf)}${resource("HF",r.links?.hfPaper)}${resource("Code",r.links?.code)}${resource("Data",r.links?.data)}</div></div><div class="card-actions"><button class="save ${saved?"saved":""}" aria-pressed="${saved}">${saved?"Saved":"Save"}</button><button class="details" aria-expanded="false">View details ↓</button></div><div class="details-panel" hidden><div class="facts">${publication?`<div><small>PUBLICATION</small><strong>${escapeHtml(publication)}</strong></div>`:""}<div><small>HF PAPER</small><strong>${fmt(r.attention?.hfPaperUpvotes)} votes</strong></div><div><small>GITHUB</small><strong>${fmt(r.attention?.githubStars)} stars</strong></div><div><small>HF DATASET</small><strong>${fmt(r.attention?.hfDatasetDownloads)} downloads · ${dsRank}</strong></div></div><p class="evidence">${escapeHtml(r.evidence?.snippet||"Source evidence unavailable.")}</p></div></article>`;}
function renderRadar(){if(!state.manifest)return;let rows=state.benchmarks.filter(r=>state.savedOnly||eligible(r)).filter(r=>!state.savedOnly||state.saved.has(r.id)).filter(r=>!state.domain||r.primaryDomain===state.domain).filter(r=>!state.search||[r.name,r.oneLine,r.primaryDomain,...(r.topics||[])].join(" ").toLowerCase().includes(state.search));rows.sort((a,b)=>state.sort==="newest"?b.releasedAt.localeCompare(a.releasedAt):(b.ranking?.[state.window]?.score??-1)-(a.ranking?.[state.window]?.score??-1)||b.releasedAt.localeCompare(a.releasedAt));$("benchmark-list").innerHTML=rows.length?rows.slice(0,state.visible).map(card).join(""):`<div class="empty">${state.savedOnly?"No saved benchmarks. Save a benchmark from Radar to keep it here.":"No benchmarks in this view."}</div>`;$("result-count").textContent=`${rows.length} results`;$("feed-title").textContent=state.savedOnly?"Saved benchmarks":state.sort==="newest"?"New benchmark releases":"Rising benchmarks";$("load-more").hidden=state.visible>=rows.length;$("updated").textContent=`Updated ${state.manifest.dataAsOf}`;$("summary").innerHTML=state.savedOnly?`<strong>${state.saved.size}</strong><span>saved in<br>this browser</span>`:`<strong>${state.manifest.recordCount}</strong><span>releases indexed<br>past 90 days</span>`;$("saved-count").textContent=state.saved.size;bindCards();}
function bindCards(){document.querySelectorAll(".benchmark-card").forEach(el=>{const id=el.dataset.id;el.querySelector(".save").onclick=e=>{if(state.saved.has(id))state.saved.delete(id);else state.saved.add(id);localStorage.setItem("benchmark-radar:watchlist:v1",JSON.stringify([...state.saved]));e.currentTarget.classList.toggle("saved",state.saved.has(id));e.currentTarget.textContent=state.saved.has(id)?"Saved":"Save";e.currentTarget.setAttribute("aria-pressed",String(state.saved.has(id)));$("saved-count").textContent=state.saved.size;if(state.savedOnly)renderRadar();};el.querySelector(".details").onclick=e=>{const p=el.querySelector(".details-panel"),open=p.hidden;p.hidden=!open;e.currentTarget.textContent=open?"Close details ↑":"View details ↓";e.currentTarget.setAttribute("aria-expanded",String(open));};});}
function lineSvg(points){const w=900,h=270,p={l:38,r:18,t:16,b:32},max=Math.max(1,...points.map(x=>x.count)),x=i=>p.l+(points.length===1?0:i*(w-p.l-p.r)/(points.length-1)),y=v=>p.t+(max-v)*(h-p.t-p.b)/max;const coords=points.map((d,i)=>`${x(i)},${y(d.count)}`).join(" ");const area=`${p.l},${h-p.b} ${coords} ${x(points.length-1)},${h-p.b}`;const grid=[0,.5,1].map(q=>`<line class="grid-line" x1="${p.l}" y1="${y(max*q)}" x2="${w-p.r}" y2="${y(max*q)}"/><text class="axis-label" x="0" y="${y(max*q)+4}">${Math.round(max*q)}</text>`).join("");const labels=points.map((d,i)=>i===0||i===points.length-1||i===Math.floor(points.length/2)?`<text class="axis-label" text-anchor="middle" x="${x(i)}" y="${h-8}">${d.week.slice(5)}</text>`:"").join("");const dots=points.map((d,i)=>`<circle class="chart-dot" cx="${x(i)}" cy="${y(d.count)}" r="3"><title>${d.week}: ${d.count}</title></circle>`).join("");return `<svg viewBox="0 0 ${w} ${h}" aria-hidden="true">${grid}<polygon class="chart-area" points="${area}"/><polyline class="chart-line" points="${coords}"/>${dots}${labels}</svg>`;}
function renderTrend(){if(!state.trends)return;const domain=$("trend-domain").value||state.trends.domains[0]?.domain;const item=state.trends.domains.find(d=>d.domain===domain)||state.trends.domains[0];const days=Number(document.querySelector("#trend-window .active")?.dataset.days||90);const points=item.weeklyNewFamilies.slice(days===30?-5:0);$("chart-title").textContent=item.domain;$("recent-count").textContent=fmt(item.recent30);const pct=item.previous30?Math.round((item.recent30-item.previous30)*100/item.previous30):null;$("growth-value").textContent=pct==null?"New":`${pct>=0?"+":""}${pct}%`;$("trend-confidence").textContent=`${item.confidence} confidence · ${item.evidenceCount60d} releases / 60d`;$("line-chart").innerHTML=lineSvg(points);$("domain-table").innerHTML=state.trends.domains.slice(0,10).map(d=>{const change=d.previous30?Math.round((d.recent30-d.previous30)*100/d.previous30):null;return `<div class="domain-row"><strong>${escapeHtml(d.domain)}</strong><span>${d.recent30} releases · 30d</span><span class="growth">${change==null?"New":`${change>=0?"+":""}${change}%`}</span></div>`}).join("");}
function setup(){const domains=[...new Set(state.benchmarks.map(r=>r.primaryDomain))].sort();$("domain").innerHTML+=domains.map(d=>`<option>${escapeHtml(d)}</option>`).join("");$("trend-domain").innerHTML=state.trends.domains.map(d=>`<option>${escapeHtml(d.domain)}</option>`).join("");document.querySelectorAll("#window-controls button").forEach(b=>b.onclick=()=>{state.window=b.dataset.window;state.visible=60;document.querySelectorAll("#window-controls button").forEach(x=>x.classList.toggle("active",x===b));renderRadar();});document.querySelectorAll("#sort-controls button").forEach(b=>b.onclick=()=>{state.sort=b.dataset.sort;document.querySelectorAll("#sort-controls button").forEach(x=>x.classList.toggle("active",x===b));renderRadar();});$("filter-toggle").onclick=()=>{$("filter-panel").hidden=!$("filter-panel").hidden;};$("search").oninput=e=>{state.search=e.target.value.toLowerCase();renderRadar();};$("domain").onchange=e=>{state.domain=e.target.value;renderRadar();};$("clear-filters").onclick=()=>{state.search="";state.domain="";$("search").value="";$("domain").value="";renderRadar();};$("load-more").onclick=()=>{state.visible+=60;renderRadar();};$("trend-domain").onchange=renderTrend;document.querySelectorAll("#trend-window button").forEach(b=>b.onclick=()=>{document.querySelectorAll("#trend-window button").forEach(x=>x.classList.toggle("active",x===b));renderTrend();});window.addEventListener("hashchange",route);renderRadar();route();}
Promise.all([fetch("./data/benchmarks_index.json").then(r=>r.json()),fetch("./data/domain_trends.json").then(r=>r.json())]).then(([b,t])=>{state.benchmarks=b.records;state.manifest=b.manifest;state.trends=t;setup();}).catch(()=>{$("benchmark-list").innerHTML='<div class="empty">Data could not be loaded. Please refresh later.</div>';});
